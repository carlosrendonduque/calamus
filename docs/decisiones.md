# Decisiones de calamus

Documento de trabajo interno. Registra qué se decidió, cuándo y por qué, incluyendo lo que se descartó. Las revisiones se anotan como filas nuevas que dicen a qué decisión enmiendan; no se edita lo ya escrito.

## Reglas permanentes de diseño

No son decisiones fechadas: son restricciones que la librería no negocia y contra las que se mide cualquier cambio. Vienen del principio rector del ecosistema — herramientas agnósticas, obras aisladas — descrito en la sección 2.1 de `PROYECTO-vision-narrativa-y-tecnica.md`.

- **Cero dependencias de runtime.** Aparte de `react` y `react-dom` como *peer dependencies*, `calamus` no arrastra nada al proyecto que la consume. Todo lo que hace (paginación, medición, transiciones, tokens de tema) se resuelve con la plataforma. Una dependencia nueva en `dependencies` es una decisión que hay que justificar y registrar aquí.
- **Agnóstico del contenido.** La librería nunca lleva material específico de ninguna obra literaria: ni texto, ni nombres propios, ni esquemas de datos modelados sobre una obra concreta. Si algo de una obra aparece dentro de la herramienta se trata como contaminación y se retira. Los ejemplos, la demo y las pruebas usan material genérico o de dominio público.

## Salida a público (2026-09-24)

| # | Tema | Decisión | Fecha |
|---|---|---|---|
| 1 | Fuga narrativa en `calamus-sandbox` | **El repositorio pasa a privado.** Estaba público en GitHub con material narrativo de la obra inédita: prosa literal, nombres de personajes y de lugares, y la taxonomía del canon — capas narrativas, actos y costes — declarada como tipos en su `SceneContract`. (Los identificadores concretos no se transcriben aquí: este documento sí es público, y repetirlos anularía el propósito de la decisión. Están en el repo privado y en el historial de la conversación que la motivó.) Eso contradice de frente el principio de aislamiento del propio ecosistema. Un force push no deshace lo que ya estuvo expuesto, así que la prioridad era **cortar la exposición siguiente**, no limpiar la pasada. Ya ejecutado: el repo figura `PRIVATE`, con cero forks y cero stars, así que no se pierde nada al cerrarlo. Qué hacer con el repo a largo plazo queda abierto (ver Pendiente) | 2026-09-24 |
| 2 | Corpus de la demo | **La demo pública usa solo textos de dominio público.** Sigue el precedente de Conato, la herramienta hermana, que demuestra con *Don Quijote*. Sale la muestra original en español del playground ("Cuaderno del pasillo interior"), que era material propio y por tanto incompatible con la regla de agnosticismo. Entran: *Tristram Shandy* de Sterne (1759-67) para `scroll`, `book` y `terminal`; *Un coup de dés* de Mallarmé (1897) para `editorial`; *Calligrammes* de Apollinaire (1918) para `hypertext`. Los dos franceses van **en original**, no traducidos: las traducciones pueden seguir con derechos vigentes y, en esas dos obras, la tipografía *es* la obra, así que una traducción no serviría igual. Tristram Shandy se elige porque es el antecesor tipográfico de lo que hace la librería y está en la misma bibliografía (la literatura ergódica de Aarseth) que el documento de visión del proyecto | 2026-09-24 |
| 3 | Reescritura de historia y force push en `calamus` | **Se reescribe la historia de `calamus` con `git filter-repo --mailmap`.** El alcance es solo este repo; el sandbox queda fuera porque su problema era otro (decisión 1) y su destino sigue abierto. **18 de los 20 commits** figuran a nombre de una identidad de terceros, `PR Writer <expert897741@reveloexperts.com>`, residuo de trabajo freelance ajeno y visible en público — y no son commits menores: son los cinco modos, los dos motores de paginación, la navegación por teclado y swipe, las transiciones y el tiempo de lectura. Es decir, en una pieza de portafolio pública el 90% del trabajo aparecía firmado por otra persona. El mailmap unifica además la variante `66224372+carlosrendonduque@users.noreply.github.com` y el nombre `Carlos Rendon`, de modo que los 20 commits queden bajo `Carlos <carlos.rendon.duque@gmail.com>`, la misma identidad que usa la landing. Corrección de una creencia previa: **la reescritura no pierde trabajo ni fechas.** Se conservan las fechas de autor, los mensajes y el contenido; lo único que cambia son los hashes. Y como el grafo de contribuciones de GitHub lee la fecha de autor, la reescritura *gana* atribución para esos 18 commits en lugar de perderla. La salvedad real es otra: después de un force push GitHub mantiene los objetos viejos alcanzables un tiempo. Por eso esto es suficiente para `calamus` — ahí no hay nada secreto, solo un correo equivocado — y no lo es para el sandbox, donde había una fuga narrativa real, que por eso se cierra (decisión 1) en vez de reescribirse. Antes de tocar nada se hace copia espejo en `~/Documents/repos/_backups/`, igual que la `carlosrendon-landing-pre-rewrite.git` ya existente (hecha: `_backups/calamus-pre-rewrite.git`). La causa raíz está descartada: la config global de git ya apunta a la identidad correcta, así que es residuo histórico y no se repetirá; `conato` está limpio | 2026-09-24 |
| 4 | Idioma de la documentación | **La documentación de calamus va solo en inglés.** La landing es bilingüe EN+ES porque ahí el público es comercial y local; una librería de código abierto se documenta en inglés, que es lo normal en el ecosistema y lo que espera quien llega desde npm o GitHub. Este registro de decisiones se queda en español porque es documento de trabajo interno, no copia publicada | 2026-09-24 |
| 5 | Versión y publicación en npm | **La versión se queda en `0.1.0`.** Documentación, pruebas y CI no cambian la API pública, así que no justifican subir semver. `package.json` recibe la metadata npm completa (descripción, repositorio, licencia, keywords, campos de publicación) para que publicar sea posible más adelante, pero **el paquete no se publica en npm todavía**: se consume por git o por `file:`. Reversible: publicar es un paso aparte que no obliga a rehacer nada de esto | 2026-09-24 |
| 6 | Pruebas reales en lugar del humo | **Se reemplaza `tests/smoke.test.ts` por pruebas de la lógica pura.** El archivo era `expect(1 + 1).toBe(2)` con un comentario admitiendo que era solo para verificar el arnés; en un repo público eso es peor que no tener pruebas, porque promete cobertura que no existe. La lógica pura — paginación de los modos `book` y `editorial`, conteo de palabras, tiempo de lectura — se extrae a módulos internos y se prueba de verdad. Restricción que definió el alcance: **no hay `jsdom` ni testing-library instalados y no se van a añadir** (regla de cero dependencias), así que las pruebas cubren funciones puras en entorno node, no componentes renderizados | 2026-09-24 |
| 7 | No se unen `calamus` y `calamus-sandbox` | **Se descarta fusionar los dos repos en uno nuevo.** Se consideró y se rechaza por tres razones. Primera: el sandbox es justamente donde vive el material narrativo de la obra, así que fusionar metería en el repo público lo único que hay que sacar de él. Segunda: el argumento de venta de la librería — y la copia ya publicada en la landing — es que es agnóstica del contenido, y un repo que lleva a la vista un contrato de escena modelado sobre una obra concreta lo desmiente solo. Tercera: un repo nuevo es una URL nueva, rompe el enlace que la landing ya publica y tira 20 commits de historia real que sirven como evidencia de método. Lo que se acepta en su lugar: calamus tiene su propia demo pública genérica (decisión 2) y el sandbox deja de ser público (decisión 1) | 2026-09-24 |
| 8 | Textos de interfaz y superficie de i18n | **Todos los textos visibles pasan a un objeto `labels` con defaults en inglés.** La librería tenía la interfaz hardcodeada en español y sin forma de cambiarla: `Pagina ${n} de ${total}` en el modo libro, `Hoja ${n} de ${total}` en editorial, y `"min de lectura"` como default de `readingTimeLabel` — es decir, español mal escrito (sin tilde) en una librería que se documenta en inglés. Se descartó traducir los strings sin más, porque dejaba el mismo problema para cualquier otro idioma y arreglarlo después también sería breaking change. Entra el tipo `ReaderLabels` con `page`, `sheet` y `readingTime`, y **sale el prop `readingTimeLabel`**: breaking change asumido ahora, cuando el paquete no está publicado y el único consumidor era el sandbox que ya es privado. Enmienda el hueco de i18n que el README documentaba como conocido | 2026-09-24 |
| 9 | Prop `lang` | **`ReaderProps` acepta `lang`, que se reenvía al elemento raíz.** Dos de los tres textos de la demo son franceses, y sin esto un lector de pantalla los anuncia en inglés y la separación de sílabas usa reglas inglesas. Para una librería cuyo argumento es leer narrativa no anglófona, era un defecto de accesibilidad en la propia demo | 2026-09-24 |
| 10 | Bug de columnas en el paginador editorial | **Se corrige: las salidas tempranas respetan `columnCount`.** Con cuerpo vacío o altura no positiva devolvían una sola columna, rompiendo la invariante `sheet.length === columnCount` que sí cumple el camino principal. Efecto visible: en pantallas de 768px o más, la hoja se renderizaba a una columna y saltaba a dos al llegar la medición real. Se descartó dejarlo documentado como hueco conocido: es justo el detalle que se nota en una demo pública. Los tres tests que fijaban el comportamiento anterior se actualizaron al corregido | 2026-09-24 |
| 11 | La demo vive en GitHub Pages | **El playground se publica en `https://carlosrendonduque.github.io/calamus/`** con un workflow calcado del de la landing. Antes solo corría en local con `npm run dev`, así que nadie que llegara desde el portafolio podía ver los cinco modos funcionando — raro para una librería de lectura. Se descartaron StackBlitz (depende de un tercero y no es URL propia) y dejarlo solo local. Detalle técnico: la demo compila a `dist-demo/` y no a `dist/`, porque `tsup` tiene `clean: true` sobre `dist/` y un build de Vite ahí borraría la librería. Requiere una acción manual una vez: Settings → Pages → Source = "GitHub Actions" | 2026-09-24 |
| 12 | Destino de `calamus-sandbox` | **Se queda privado y sin más trabajo.** Cierra el pendiente abierto por la decisión 1. Ya cumplió su función — madurar el consumo de la librería antes de integrarla — y su runbook v1 está agotado (paso 11 de 11, handoff implementado). Se descartó archivarlo en GitHub, que es más definitivo y obligaría a desarchivar para retomarlo, y se descartó reescribirle la historia para sacar el material narrativo: es trabajo real sobre un repo que ya no es público, y solo valdría la pena si se pensara volver a publicarlo. Queda disponible por si se retoma la experiencia diegética. Sus 2 commits con identidad de terceros se quedan como están, por la misma razón | 2026-09-24 |
| 13 | Enlace de la demo en la landing | **La tarjeta de calamus enlaza la demo además del repo.** Cierra el pendiente correspondiente. El tipo `Project` de la landing ya tenía el campo `live` y `ProjectCard.astro` ya lo renderizaba, así que fue una línea en `src/data/content.ts`; sale como "Live" en inglés y "En vivo" en español. Publicado y verificado en `carlosrendon.co` | 2026-09-24 |
| 14 | Accesibilidad: se cierran los ocho huecos | **Se resuelven los ocho huecos que el README documentaba como conocidos**, en lugar de dejarlos como issues abiertos. Cierra el pendiente correspondiente. Dos decisiones de criterio dentro de esto, ambas contraintuitivas y documentadas en el README: (a) las barras de progreso **siguen siendo decorativas** con un equivalente textual para lectores de pantalla, en vez de `role="progressbar"`, porque algunos lectores narran cada cambio de valor y eso hablaría sin parar mientras se recorre la prosa — peor que no exponer nada; (b) el cambio de página se anuncia con **una región oculta aparte**, no marcando la línea de estado visible como `aria-live`, porque al montar el lector renderiza `Page 1 of 1` y repagina un frame después, así que una línea viva anunciaría ese cálculo de layout —y cada resize— como si el usuario hubiera navegado. La región se queda vacía hasta el primer giro real. Coste aceptado en (b): tras el primer giro el texto de estado existe dos veces en el árbol de accesibilidad. Se eligió `role="group"` y no `<nav>` para los controles de página: la librería se incrusta en una página ajena y crear un landmark para dos botones contamina el mapa de landmarks del anfitrión | 2026-09-24 |
| 15 | Ningún string accesible queda hardcodeado | **`ReaderLabels` crece a once claves y absorbe todos los nombres accesibles**, no solo los textos visibles. Enmienda la decisión 8, que solo había cubierto lo visible. Se logró **sin añadir props**: `ReaderProps` queda intacto y `ReaderLabels` ya se exportaba, así que las claves nuevas viajan con él. Queda deliberadamente fuera el *chrome* tipográfico de cada modo (`reader --scroll`, `less --book`, `$ cat document.txt`, `[EOF]`, `viewer --editorial`): son diseño visual del modo, no copia de interfaz, y están listados como hueco conocido en lugar de configurados | 2026-09-24 |

## Pendiente

- Sin pruebas automáticas de accesibilidad: el suite corre en Node sin DOM, así que roles, regiones vivas y foco se verifican leyendo el código y a mano. Añadir un entorno DOM significa una dependencia de desarrollo, que el proyecto evita
- El *chrome* tipográfico de los modos sigue en inglés y no es configurable (decisión 15). Si alguna obra lo necesita en otro idioma, son seis claves más en `ReaderLabels`
- Publicar en npm: **no por ahora**, reconfirmado el 2026-09-24 (decisión 5). La API de `labels` tiene horas de vida y los arreglos de accesibilidad pueden añadir props; publicar congelaría eso en semver. El README instala por git y la metadata ya está lista. Revisar cuando la API se asiente
